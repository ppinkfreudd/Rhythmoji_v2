// displayOutput.js
document.addEventListener('DOMContentLoaded', function () {
    // Fetch the image URL passed from the server and update the image src
    const imageUrl = ''; // You need to fetch or receive this from the server somehow
    document.getElementById('rhythmoji-image').src = imageUrl;
});
